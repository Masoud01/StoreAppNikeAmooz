@model List<Product>
@foreach (var item in Model)
{
    <div>

        <h4>
            @item.Name
                <span><small>@item.Price</small></span>
        </h4>
    </div>
    <div>@item.Description</div>
}
